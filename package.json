{
  "name": "classification-encoder",
  "version": "0.1.0",
  "description": "JavaScript library to encode/decode classification strings to/from unsigned 32-bit integers",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:verbose": "jest --verbose",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint src/ tests/ --fix",
    "build": "npm run lint && npm run test",
    "dev": "node src/index.js"
  },
  "keywords": [
    "classification",
    "encoder",
    "decoder",
    "bitmask",
    "security"
  ],
  "author": "",
  "license": "GPL-3.0",
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "babel-jest": "^29.7.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
